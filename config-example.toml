# 需要修改的网站，支持*等匹配符号
domains = ['aaa.com', 'bbb.com', 'ccc.com']

# ad_guard_home的IP地址
ad_guard_home_url = '192.168.1.1'

# ad_guard_home的管理端口
ad_guard_home_port = 3000

# ad_guard_home的用户名
ad_guard_home_username = 'username'

# ad_guard_home的密码
ad_guard_home_passwd = 'password'

# ping的次数
test_ping_count = '10'

# 丢包率上限
test_packet_loss = '0.2'

# 自建的测速下载地址，不改也行。具体可以看这里 https://github.com/XIU2/CloudflareSpeedTest/issues/168
test_url = 'https://alist.wwtt.xyz/d/Unraid/Backup/SystemBackup/libvirt/libvirt.img?sign=v5s_EpqwzUdQL4U20nBXbMjn_fZh20cKfc_wWNpdo8Q=:0'

# 最高延迟的秒数
test_max_delay = 2.0